exports.main = async (infoStorage) => {
    // const codename = infoStorage.get('codename')

    // I have no idea what to do with this.
    // I've never seen this one return anything ever ???????

    return [2, '']
}
